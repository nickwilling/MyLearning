server:
  port: 8080
  servlet:
      context-path: /news

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/news?useUnicode=true&characterEncoding=utf8
    username: root
    password: 60002314
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  resources:
    static-locations: classpath:/resources/,classpath:/static/,classpath:/public/
